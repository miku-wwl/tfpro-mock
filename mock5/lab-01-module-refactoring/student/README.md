# Student Workspace

The active starter is the monolithic root in this directory. The four directories under `modules/` are draft refactoring material: they are not called by the starter and contain interface defects that must be repaired against the root lab specification.

Do not place solution notes or state migration commands in this directory. The final deliverable remains under `student/` using the required `infra/shared`, `infra/application`, and `modules/*` layout.
