aws_region          = "us-east-1"
localstack_endpoint = "http://localhost:4566"
project_code        = "harbor-grid"

# setup.sh/setup.ps1 creates backend.auto.tfvars.json with the real LocalStack bucket name.
