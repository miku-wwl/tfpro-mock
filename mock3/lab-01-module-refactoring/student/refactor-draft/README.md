# Inherited refactor draft

These fragments are intentionally incomplete and incorrect. They are supplied as evidence of a failed prior refactor. Adapt their intent into the required final layout; do not rename them to `.tf` unchanged.

Defects are independent. A fragment that parses or can contact LocalStack may still violate the identity contract.
